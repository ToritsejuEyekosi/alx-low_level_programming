#include "main.h"
/**
 * _isdigit - checks if character is a digit
 * @x: the number to be checked
 * Return: 1 if character is a digit else return 0 for anything else
 */
int _isdigit(int x)
{
if (x >= 48 && x <= 57)
{return (1); }
return (0);
}
